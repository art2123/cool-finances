[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Worker: без healthcheck, без uvicorn. RUN_MODE=worker задаётся в Variables.
startCommand = ""
restartPolicyType = "ON_FAILURE"
