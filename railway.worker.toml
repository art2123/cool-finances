[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
startCommand = ""
# Явно отключаем healthcheck (worker не отдаёт HTTP)
healthcheckPath = ""
healthcheckTimeout = 0
restartPolicyType = "ON_FAILURE"
