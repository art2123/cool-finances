[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
# Worker — не веб-сервер, healthcheck отключён (нет /health)
startCommand = "python -m src.main worker"
restartPolicyType = "ON_FAILURE"
