# Force Railway to use Dockerfile instead of auto-detect
[phases.setup]
nixPkgs = ["python312", "gcc", "postgresql"]

[phases.install]
cmds = ["pip install -r requirements.txt"]

[start]
cmd = "uvicorn src.api.app:app --host 0.0.0.0 --port ${PORT:-8000}"
